# Security configuration for Digital Ocean builds
save-exact=true
audit=true
audit-level=high

# CRITICAL: Prevent malicious scripts from running during install
ignore-scripts=true

# Use lockfile for security
package-lock=true

# Timeout for slow/hanging installs
network-timeout=30000
